apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.adapter.listenPort }}
  selector:
    app: {{ .Release.Name }}